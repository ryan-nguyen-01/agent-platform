# Project conventions (Kiro fills this on scan)

Kiro records the project's real coding conventions here so later sessions reuse them instead of
rescanning. Update as you discover more. This describes the project as it IS — do not impose new style.

## Frontend

```text
- Framework + version:
- Component style (function/class):
- File/folder layout:
- Naming (components, hooks, files):
- State management:
- Styling (CSS modules / Tailwind / styled-components / ...):
- Shared/common components (where they live, how to import):
- Custom hooks (location + patterns):
- Data fetching (fetch / axios / react-query / RTK Query / ...):
- Import ordering / path aliases:
```

## Backend

```text
- Language + framework:
- Layering (controller/service/repository or routes/handlers):
- Error handling + response shape:
- Validation approach:
- Auth pattern:
- DB access (ORM / query builder / raw):
- Config + env handling:
- Logging:
- Test style + runner:
- Module boundaries / naming:
```

## Shared

```text
- Lint/format config:
- Commit/branch conventions:
- Build/run commands:
- Anything surprising / gotchas:
```
