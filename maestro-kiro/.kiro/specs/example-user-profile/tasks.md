# Tasks — Trang hồ sơ người dùng (ví dụ)

- [ ] 1. Migration thêm cột `display_name`, `avatar_url` vào `users` _(Req 1, 2)_
- [ ] 2. BE: `ProfileDto`, `UpdateProfileRequest` + validation _(Req 2)_
- [ ] 3. BE: `ProfileService` (get/update) + `ProfileController` (`GET`/`PUT /api/me/profile`) _(Req 1, 2)_
- [ ] 4. BE: test service + controller (200/400/401) _(Req 1, 2)_
- [ ] 5. FE: `ProfilePage` dùng `Card`/`Button`/`TextField` + `useCurrentUser`/`useToast` _(Req 1, 2)_
- [ ] 6. FE: React Query query + mutation (invalidate sau khi lưu) _(Req 2)_
- [ ] 7. FE: validation form (rỗng / > 50 ký tự) _(Req 2)_
- [ ] 8. e2e Playwright: xem → sửa → lưu → thấy thông báo _(Req 1, 2)_

> `[ ]` chưa làm · `[~]` đang làm · `[x]` xong. Không tick xong nếu chưa có bằng chứng (build/test pass).
