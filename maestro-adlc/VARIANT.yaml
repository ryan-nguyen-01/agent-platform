name: maestro-adlc
display_name: "Maestro ADLC"
purpose: "Agent Development Lifecycle: building agentic products (agents with mission, tools, memory, planning loop) with a behavior eval gate before done."
methodology_default: eval-driven-ai
skills: 164
work_roots: [agents, evals, app, tests]
note: |
  Self-contained workspace — edit this bundle directly. Agent-native folder tree: agents/ holds the
  agents you build (per-agent agent.md mission, tools.yaml, prompts/, memory.yaml, knowledge/),
  evals/ holds behavior eval suites (the eval gate: task success, trajectory, tool-call, safety),
  app/ holds the host application that serves the agents, tests/ holds code tests. Product docs in docs/.
