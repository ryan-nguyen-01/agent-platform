name: maestro-brownfield
display_name: "Maestro Brownfield"
purpose: "Maintain an EXISTING, running project: deep onboarding first, execute assigned tasks precisely, ask when unclear — never infer."
methodology_default: risk-based-routing
skills: 227
work_roots: [source, intake]
note: |
  Self-contained workspace — edit this bundle directly. Folder tree: source/ holds the existing
  project's code (one folder per service/app/package), intake/ holds raw user material (specs,
  bug reports, logs, dumps) awaiting triage.
