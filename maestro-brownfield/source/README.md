# source/

The existing project's code you maintain. Put the project here — one folder per service/app/package,
keeping the repo's own internal layout. Onboarding scans this root and registers each component in
`.maestro/registry/components.yaml`.

This folder holds CODE only. Raw material the user drops in (specs, bug reports, logs, dumps) goes in
`intake/`, not here.
